const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://postgres:helpdeskyareu@localhost:5432/helpdesk-kominfo?schema=public' }); client.connect().then(async () => { const t = await client.query(SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'services'); console.log(t.rows); }).catch(e=>console.log(e)).finally(()=>client.end());
